#!/bin/sh

set -e

wm=enlightenment_start

case "$1" in
	remove|upgrade|failed-upgrade)
		update-alternatives  --remove x-window-manager /usr/bin/$wm
		update-alternatives  --remove x-session-manager /usr/bin/$wm
		;;
	*)
		;;
esac

#DEBHELPER#
